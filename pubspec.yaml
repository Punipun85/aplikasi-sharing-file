name: secureshare
description: SecureShare multi-platform secure file sharing app.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  app_links: ^7.1.1
  cryptography: ^2.7.0
  crypto: ^3.0.7
  cupertino_icons: ^1.0.8
  file_picker: ^11.0.2
  flutter_dotenv: ^6.0.1
  go_router: ^17.3.0
  http: ^1.6.0
  intl: ^0.20.2
  provider: ^6.1.5+1
  supabase_flutter: ^2.14.1
  universal_html: ^2.2.4
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
